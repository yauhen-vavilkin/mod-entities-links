{
  "type": "string",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "description": "Resource event operation type - one of [update, delete]. Other are not supported",
  "enum": [
    "UPDATE",
    "DELETE"
  ]
}
